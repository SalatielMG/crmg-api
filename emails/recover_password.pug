extends base.pug

block title
    title Recuperar contraseña
block content
    .container.my-5
        h1 Hola #{user.name}
        if user.pin
            p Pin: #{user.pin}
        p.important Estás recibiendo este correo porque se ha solicitado un cambio de contraseña para su cuenta.
        a(href=`${ resetLink }` class="btn btn-primary") Cambiar contraseña